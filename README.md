# Webhook Delivery Platform

A self-hostable webhook delivery service. Customers register destination URLs, send events to the API, and the system reliably delivers them as signed HTTP POSTs with retries and observability.

## Status
Phase 0 — in progress.